const SearchBar = () => {
  {
    /* SearchBar: un input de búsqueda para encontrar países por nombre. */
  }

  return (
    <form>
      <label htmlFor="email">Busqueda</label>
      <input type="" name="busqueda" />
      <label >
        password
      </label>
      <input type="" />
      <button>BUSCAR</button>
    </form>
  );
};

export default SearchBar;